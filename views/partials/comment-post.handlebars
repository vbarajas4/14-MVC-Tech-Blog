<div class="card row mb-4 post-list">
  <div class="col-md-5">
    <div>
       <p>
        {{comment_text}}
       </p>  
    </div>
    <p>Commented by {{user.username}} on {{format_date createdAt}}</p>
  </div>
</div>

{{!-- <div>
 {{#if (compare_users user.id user_id)}}

 <input type="hidden" name="comment-id" value="{{id}}" />

  <button onclick = "deleteComment">Delete</button>
  {{/if}}
</div>

<script src = "/js/delete-comment.js"></script> --}}

